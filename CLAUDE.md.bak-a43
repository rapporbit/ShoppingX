# CLAUDE.md

本文件指导在本仓库中按照 `refdocs/` 所描述的框架，从零搭建 **ShoppingX —— 全球电商搜索 Agent**。

**如何使用 refdocs（务必先读这段）：**
`refdocs/` 是一套 18 章的教学文档，但**是从网上扒下来的，代码片段往往不全、可能过时或有错**。因此它的定位是：
- ✅ **设计思想、架构选型、模式、各层的「为什么」** —— 以 refdocs 为准，整体框架要与之保持一致。
- ⚠️ **具体代码（签名、常量、字段名、流程细节）** —— 只作参考，**不要一板一眼照抄**。实现时按真实可运行、当前依赖版本、本仓库实际数据来写；refdocs 里不合理 / 残缺 / 跑不通的地方，**主动更正修改**，并让代码自洽。

一句话：**对齐框架与思想，不照抄代码。** 本文件给的目录/签名/常量是落地约定的「建议初值」，可在实现中按需调整，只要不破坏第 2 节的核心架构与硬约束。

---

## 0. 与 Claude 协作的硬约束

- **禁止为调研派发多个 sub agent（Agent/Task 工具）**。无论是并行还是串行，凡是「调研/探索/查资料」类需求，一律由 Claude 自己直接读文件、grep、搜索完成，不得拆分给多个 sub agent 去跑。
- **每次回复用户的第一句话必须是「Yes Sir!」**，然后再展开正文。
- **控制单次工具调用的输出体量**，避免流式响应中途 stall（`Response stalled mid-stream`）：
  - 单次 Write 不超过约 120 行；更长的文件每个任务逐步完成而非一次性批量输出。
  - grep / 目录遍历务必收敛匹配范围或加 `head`，不要一次吐回几百行结果。
  - 会话过长（几十次工具调用）时主动提示用户开新会话，而不是硬撑。

---

## 1. 项目目标

做一个面向全球电商场景的**对话式购物 Agent**。用户用自然语言提出购物意图（如「想买便宜又抗造的旅行三件套，预算 300，不要塑料的，喜欢小众」），系统跨多个平台并行检索、比价、算关税运费、按偏好精挑，最后给一份**带购买理由的清单**，并把用户偏好沉淀为长期记忆。

它不是完整电商生产系统。**不覆盖**：真实平台 OAuth、下单/支付/物流、精细汇率对账、多模态图像检索、反爬风控、库存一致性、隐私合规、**LLM 训练 / 微调（SFT、RL、数据飞轮）**。聚焦 **Agent 主链路 + 工程基础设施**；模型层只用现成 API，评测仅做离线打分、不回流训练。

---

## 2. 核心架构

### 2.1 AgentLoop 范式
主智能体是一个有状态循环：**Think → Act → Observe → Reflect**，循环到模型自判「信息已足够」才终止，而非外部写死步数。

- **Think**：拆解意图、判断还缺什么信息
- **Act**：调用工具或 `dispatch_tool` 派发子 Agent
- **Observe**：读取工具返回（关注结构化字段）
- **Reflect**：够了就调终结性工具收尾；不够就回到 Think

与传统 Workflow / ReAct / DeepAgents 的区别见 `refdocs/01`。

### 2.2 Supervisor-Workers：按读写属性切分
主 Agent 是 Supervisor，**持有全部业务工具、单干优先** —— 默认自己调工具，只有三件事判断任一满足才派发 worker。worker 按**读写属性**切分，现只剩只读的 SearchAgent（TradeAgent 已在 A1 删除：440 会话 0 派发，交易工具只出确认卡、由主 Agent 直接调）：

| Agent | 工具集 | `max_iters` | 提示词段 |
| --- | --- | --- | --- |
| MainAgent（Supervisor） | 全部业务工具 + `task_dispatch` | 15 | `main_agent` |
| SearchAgent（只读 worker） | `item_search` / `web_search`（`_SEARCH_TOOLS`，全部 `is_read_only=True`；`price_compare` / `category_insight` / `shipping_calc` 是主 Agent 专属——比价 / 到手价要跨平台合流后的全局视图，见 M25 偏离记录） | 6 | `sub_agents.search` |

**三条硬规则：**
1. **读写边界是结构性的** —— SearchAgent 的 Toolkit 里根本没有写工具，不靠提示词自觉。交易工具只在主 Agent 手上，`create_order` / `cancel_order` 只出确认卡、决议只走 HTTP，`item_ids` 只收候选登记表里的 id。写工具经 `PermissionEngine` 精准 allow，**不用 `BYPASS`**。
2. **偏好由服务端注入** —— `task_dispatch` 内按 buyer 取偏好渲染成 `<buyer-preferences>` 塞给 SearchAgent。
3. **主 Agent 单干优先** —— 这是消掉 Supervisor-Workers「小事也要派一趟」硬伤的关键。

> **隔离落地口径（诚实标注）**：上下文隔离靠 `ContextVar`（`thread_id` / `session_dir`，`thread_scope` 进出 set/reset）+ 每次派发新建独立 Agent 实例。会话恢复只从 `output/<thread_id>/session.json`（`AgentState.model_dump_json()`）读回，无 history 回放腿；文件损坏即空开局。会话级偏好 P_t 存在 `state.middle_context["pt"]`，messages 表只服务前端侧栏。
>
> **同质 fork 是被推翻的中间态**：本仓 M2 / M9 做过「子 Agent = 主 Agent 完整克隆（同一份工具集、同一份 system prompt）」，实测数据原样保留在 `docs/milestones/` 作为「做过对比」的证据；`WORKER_MODE=clone|split` 开关保留，`clone` 供同框架对照实验，`split` 是默认。

**三件事判断（任一满足才派发）：**
1. **能并行** —— 多个独立子任务可同时跑（如跨平台同时 ItemSearch）
2. **要隔离** —— 子任务产生大量中间数据，不该污染主 loop 上下文
3. **链够深** —— 子任务自身需 ≥3 层工具调用

否则主 Agent 自己处理（单次工具调用 / 澄清 / 闲聊兜底 / 收尾）。

> **主 loop 的两个机制自动步（2026-09-15 延迟 round3）**：① 开局预置 planner 时并发预取 `category_insight`
> （`harness/prefill.py`）；② 检索类工具（`item_search` / `task_dispatch(search)`）成功后武装，下一次模型调用前
> 自动跑 `price_compare` + `item_picker` 并以 hint 注入（`harness/autopick.py`，套装轮 / worker / `AUTOPICK=0`
> 不自动）。两者都走 `adapter.after_tool_success` 同一条 post_tool_call 管线，控制面状态与模型亲手调一致。
> 普通轮的典型序列因此是 planner(+KB) → item_search → shopping_summary（4 次模型调用）。

派发通过 `task_dispatch(demands: str, subagent_type: Literal["search"])` 暴露给主 Agent —— 对主 Agent 而言它就是「另一个工具」：传 demands、拿最终回复。同轮多个独立子任务**一次性发起多个 `task_dispatch`**，由 `is_concurrency_safe=True` 批并发（不再有 `parallel_dispatch_tool`）。详见 `refdocs/03-1`、`14`。

**派发安全四层**（务必实现，否则会无限递归 / token 爆炸）：深度上限（worker 工具集里没有 `task_dispatch`，结构性保证）、子 Agent 超时 + `max_iters`、工具结果截断、循环检测。

---

## 3. 目标目录结构

教学最终态对应包名 `shoppingx-agent/`（本仓库根即项目根）。按此结构落地：

```
shoppingx/
├── app/                          # 后端主目录
│   ├── agent/
│   │   ├── llm.py                # get_llm() / get_fast_llm() / get_judge_llm() 模型工厂
│   │   ├── gateway.py            # GatewayThrottle（并发+起点间隔）/ ThrottledChatModel
│   │   ├── prompts.py            # 读取 prompt/prompts.yml，get_system_prompt()
│   │   ├── tool_registry.py      # BUSINESS_TOOLS, build_toolkit(role), TERMINAL_TOOLS
│   │   ├── dispatch_tool.py      # task_dispatch(demands, subagent_type)（派发入口）
│   │   ├── agents.py             # build_main_agent() / build_worker_agent(kind)
│   │   ├── permissions.py        # PermissionRule 精准放行（写工具不用 BYPASS）
│   │   ├── events.py             # AgentScope 事件 → AGUI 事件映射
│   │   └── orchestrator.py       # run_agent() 执行入口，消费 reply_stream
│   ├── harness/                  # 控制面：6 个 hook_point 的注册表与 hooks + fork_guard/token_budget/retrieval_budget/model_router 状态源
│   │   ├── middleware.py         # harness_hook(hook_point, name, priority) 注册表
│   │   ├── adapter.py            # 钩子适配：hook_point → MiddlewareBase/ToolMiddlewareBase
│   │   ├── formatter.py          # CacheAwareOpenAIFormatter（cache_control 透传）
│   │   └── hooks/                # 按关切分文件：safety/termination/budget/sequencing/validation/repetition/progress/drift/context_shaping
│   ├── api/
│   │   ├── context.py            # ContextVar: thread_id / session_dir
│   │   ├── monitor.py            # AGUI 事件统一封装
│   │   ├── connection.py         # ConnectionManager（thread_id → WebSocket 路由）
│   │   └── server.py            # FastAPI：任务 / 取消 / 上传 / 文件 / WebSocket
│   ├── tools/                    # 9 大工具（每文件一个工具，模块名=工具名）
│   ├── recall/                   # 三塔向量召回 + ANN + 汇率/关税/运费 + RAG 知识库
│   ├── memory/                   # 长期记忆 Store + 偏好注入
│   ├── eval/                     # Rubric 评测 + 召回指标
│   └── utils/                    # thread_ctx / path_utils
├── prompt/prompts.yml            # system prompt + 各工具提示词 + 偏好注入点
├── frontend/                     # React + Vite（对话框 / AGUI 可视化 / 商品卡 / 偏好面板）
├── scripts/                      # 数据生产 ETL、知识库构建、评测脚本
├── data/                         # 数据集（gitignore，用 scripts/ 可复现）
├── output/  uploaded/            # 每会话产物 / 上传目录（按 thread_id 分目录）
├── examples/                     # 1~8 章能力示例脚本
├── tests/                        # 工具 / 连接管理 / 取消任务测试
├── docker/                       # 向量库 / Redis / OpenSearch 的 docker-compose
├── pyproject.toml
└── refdocs/                      # 教学文档（事实来源，勿改）
```

> 当前仓库为初始骨架：`main.py` 仅占位；`data/` 已有 6 个平台商品 CSV（amazon / ebay / lazada / shein / shopee / walmart）与 RAG 语料；`scripts/fetch_milistu.py` 拉取 Amazon 商品数据集。`app/`、`frontend/` 等尚未创建——按上表逐步建。

---

## 4. 技术栈

| 模块 | 技术栈 | 作用 |
| --- | --- | --- |
| Agent 范式 | AgentScope 2.0 `Agent` + `ReActConfig` | 主循环 Think→Act→Observe→Reflect |
| 多 Agent | Supervisor-Workers 读写切分：SearchAgent（只读），交易工具留在主 Agent；独立 thread_id + 独立 Agent 实例（见 §2.2） | 跨平台并行 / 上下文隔离 / 结构性权限边界 |
| 模型与工具 | AgentScope `OpenAIChatModel` + `FunctionTool` / `Toolkit`；`MiddlewareBase` / `ToolMiddlewareBase` 挂控制面 | 统一大模型、工具声明、钩子面 |
| 向量召回 | 预训练 Embedding（BGE-M3 等，API/CPU）+ Qdrant（dense + payload filter） | 跨语言跨平台商品召回；**用现成模型，不自训三塔**（无 GPU）；个性化走「偏好词并入检索词」而非向量融合 |
| RAG / 应用层检索 | OpenSearch（Hybrid Query：KNN+BM25，ik 分词） | 品类知识库、长期偏好的语义+全文+标量融合 |
| 精排 | BGE-Reranker-v2-m3 cross-encoder | 召回后精排 |
| 长期记忆 | Store（Redis / Postgres / OpenSearch） | 偏好 / 黑名单 / 历史跨会话持久化 |
| 上下文压缩 | Cache Breakpoint + 自定义压缩 | 50+ 轮不爆 token，保住 Prompt Cache 命中 |
| 事件协议 | AGUI 事件流 | 前端实时可见 Agent 在做什么 |
| 后端 | FastAPI + Uvicorn + asyncio | 长任务异步、任务表、取消、文件接口 |
| 实时通信 | WebSocket + ConnectionManager | 按 thread_id 路由事件 |
| 前端 | React + Vite | 对话、事件可视化、商品卡、偏好面板 |
| 评测 | Rubrics as Rewards（P0/P1/P2 动态细则） | 每条 query 动态生成评分细则，自动 judge |
| 环境 | uv + Python 3.11 | 依赖与虚拟环境 |

---

## 5. 十五大工具 + task_dispatch

`app/tools/` 下每个工具一个文件，模块名=工具名（snake_case）。`task_dispatch` 放 `app/agent/`。全部进入 `BUSINESS_TOOLS`，由 `build_toolkit(role)` 按角色发放：**主 Agent 拿全集，SearchAgent 只拿只读子集**——两者共用同一批 `FunctionTool` 对象，切的是发放范围，不是实现。

| 工具 | 作用 | 典型调用时机 |
| --- | --- | --- |
| `planner` | 把购物意图拆成结构化字段（预算/品类/材质偏好/风格/硬约束/软偏好）；bundle 模式拆多槽位 | 复杂多约束需求先调 |
| `image_understand` | 上传参考图 → VL 模型识别品类/属性/检索词（图不进主 loop messages，结论以文本回传） | 用户附图时，先于 planner 预跑 |
| `item_search` | 单平台商品检索（三塔召回）；跨平台时同轮多派 `task_dispatch` 并行 | Act 检索 |
| `category_insight` | 品类爆款 / 典型属性（RAG 商品知识库） | 开局随 planner **预取**（`harness/prefill.py`，有品类 + 购物任务时）；模型只在要换品类时再调 |
| `price_compare` | 跨平台比价（汇率归一）+ 一步算到手价 | 检索合流后由 `harness/autopick.py` **自动跑**（普通轮）；模型仍可显式调 |
| `shipping_calc` | 关税 + 运费估算（landed cost），收货国四层解析 | 算到手价 |
| `item_picker` | 合流候选集里按用户偏好二次精挑；bundle 模式按槽位 MCKP 组合优选；每批精排封顶 `PICK_RERANK_K=15` | 普通轮随 price_compare **自动跑**（结果以 hint 注入，模型据此收尾）；套装轮由模型调 |
| `web_search` | 评测、博主推荐、价格趋势等外部事实 | 需外部信息补充 |
| `ask_user` | 向用户发澄清问题（支持单选/多选可点选 options），Future 桥接暂停 loop 等回复 | 信息不足需澄清 / bundle 组成确认 |
| `forget_preference` | 用户主动要求遗忘某条偏好 | 用户说「别记了」「忘掉 X」 |
| `chat_fallback` | 非购物意图闲聊兜底（终结性） | Think 判定非检索意图 |
| `shopping_summary` | 终结性工具，给最终清单 + 选购理由；文案 / 逐件理由 / off_intent 由**主模型在入参里给**，工具只排版（入参无 summary 才退回内部 LLM） | 拿到精选清单后收尾 |
| `create_order` | 下单（mock，无支付/物流/库存）；`confirmed=False` 只回确认卡 | 用户确认购买某几件后 |
| `query_order` | 查订单（只读，带归属校验） | 用户问「我的订单」 |
| `cancel_order` | 取消订单，仅 CONFIRMED 可取消 | 用户要求取消，须先 `query_order` |
| `task_dispatch(demands, subagent_type)` | 派 worker（`search`），派发入口/元工具 | 满足三件事判断任一 |

`TERMINAL_TOOLS = {"shopping_summary", "chat_fallback", "create_order", "cancel_order"}` —— 调用即终结循环（`query_order` 非终结）。

**角色发放**（以 `app/agent/tool_registry.py` 的 `_SEARCH_TOOLS` 为准）：SearchAgent = `item_search` / `web_search`（全 `is_read_only=True`；外加 MCP 只读白名单工具）；交易工具 `create_order` / `query_order` / `cancel_order`、`price_compare` / `category_insight` / `shipping_calc` / `item_picker` 等其余工具与 `task_dispatch` 只在主 Agent 手上。

工具实现细节、子 Agent 触发场景见 `refdocs/11`（ItemSearch）、`refdocs/12`（PriceCompare/ShippingCalc）、`refdocs/13`/`13-1`（CategoryInsight + RAG）、`refdocs/14`（主 loop 组装）。

---

## 6. 关键约定

### 6.1 工具声明
AgentScope `FunctionTool`，**async**，业务逻辑抽 `_xxx_impl` 并保留 Pydantic `*Output` 作为结构化契约，工具函数只做入参强转 + 调 impl + 包 `ToolChunk`。docstring 写给模型看（一句话摘要 + 何时调用 + 参数说明）。工具模块**禁止** `from __future__ import annotations`（schema 靠注解反射，延迟注解会让它拿到字符串）：

```python
from agentscope.tool import FunctionTool, ToolChunk
from agentscope.message import TextBlock, ToolResultState
from pydantic import BaseModel

class ItemSearchOutput(BaseModel):
    platform: str
    candidates: list[Candidate]

async def item_search(query: str, platform: str = "all", top_k: int = 20) -> ToolChunk:
    """单平台商品检索。何时调用：需要在某平台搜商品时。跨平台请同轮多派 task_dispatch 并行。"""
    out: ItemSearchOutput = await _item_search_impl(query, platform, top_k)
    return ToolChunk(
        content=[TextBlock(type="text", text=out.model_dump_json())],
        state=ToolResultState.SUCCESS,
        metadata={"schema": "ItemSearchOutput"},
    )

TOOL = FunctionTool(item_search, is_read_only=True)   # 只读工具必须标，这是权限边界的依据
```

命名：`{动词}_{名词}`（`item_search` / `price_compare`）。每个工具内用 `monitor.report_tool_start/end` 上报 AGUI 事件。工具内部报错**不要往外抛**，改回 `state=ToolResultState.ERROR` + `[error] ...` 文本，让 harness 的 `result_nudges` 能读到。

### 6.2 System Prompt 结构
XML 分块（LLM 对 XML 边界比 Markdown 标题更稳），从 `prompt/prompts.yml` 读取，至少含：`<role>`（能力边界）、`<workflow>`（Think→Act→Observe→Reflect）、`<tool_policy>`（含单干优先 + 三件事判断 + `subagent_type` 选择）、`<termination>`（**P0 —— Agent 最常见的失败是不收尾死循环**）、`<constraints>`（硬约束放结尾，位置敏感）、`<user_long_term_preferences>`（运行时从 Store 注入）。详见 `refdocs/14`、`refdocs/10`。

### 6.3 上下文与并发
- `app/api/context.py` 用 `ContextVar` 保存 `thread_id` / `session_dir`，实现多用户隔离与透明传播。
- 派发 worker 时覆盖子 `thread_id`，但**继承父 `session_dir`**（产物归同一会话目录），结束后 reset（用 `thread_scope` 上下文管理器）。
- 全链路 `async/await`。`session_dir = output/<thread_id>/`。
- 文件接口用 `safe_join` 防 `../` 路径穿越。

### 6.4 LLM
`app/agent/llm.py` 统一封装出 `ChatModelBase`：`get_llm()` 给主 + 两个 worker 共享（`lru_cache`，temperature 低），`get_fast_llm()` / `get_vision_llm()` 分别给快档与 VL，`get_judge_llm()` 给 Rubric 评测用更强模型、temperature=0。主模型是 `ThrottledChatModel`（并发 + 起点间隔闸门），限流退避用尽经 `ModelConfig.fallback_model` 回退。模型与 endpoint 走 `.env`。

---

## 7. 基础设施层要点

- **上下文压缩**（`refdocs/05`）：压缩本身交 AgentScope 框架 `compress_context`（超 `ContextConfig.trigger_ratio` 0.8 时 LLM 写 continuation summary 进 `state.summary`，随 session.json 持久化，摘要是二手上下文、有意接受）。本仓只保留 `harness/formatter.py` 给 system 段打 `cache_control`（单请求 ≤4 个、最小写入阈值、前缀精确匹配）。
- **memory/**（`refdocs/06`）：`PreferenceEntry`（key/category/content/source_session/confidence），Store 抽象（read / write / delete / `read_relevant` 语义匹配）。Think 阶段读、Reflect 阶段写（识别「不要 X」「喜欢 X」等）。后端 Redis / Postgres / OpenSearch 可选。
- **api/monitor.py**（`refdocs/07`）：AGUI 事件类型 `session_created` / `assistant_call` / `tool_start` / `tool_end` / `fork` / `task_result` / `task_cancelled` / `error`，统一 JSON 结构，经 `ConnectionManager.send_to_thread` 推给对应前端。
- **api/server.py**（`refdocs/15`）：`POST /api/task`（后台 `asyncio.create_task` 跑 `run_agent`，立即返回 thread_id）、`WS /ws/{thread_id}`、`POST /api/task/{thread_id}/cancel`、`GET /api/files/{thread_id}/{filename}`、`POST /api/upload`。维护 `active_tasks: dict[str, asyncio.Task]`。
- **recall/**（`refdocs/04-0/1/2`、`13-1`）：`TowerClient`（query/商品文本编码，**封装现成预训练 embedding（BGE-M3 等）的 API/CPU 推理，不自训三塔**）、`QdrantRecall`（Qdrant dense + 多维 payload filter；早年的 Faiss AnnClient 已移除）、`fx/duty/shipping`（汇率/关税/运费表）、`reranker`（BGE-Reranker，API/CPU 推理）、OpenSearch Hybrid（KNN 0.7 + BM25 0.3，可调）。**无 GPU 训练**：`refdocs/04-2`（Embedding/Reranker 训练）仅作背景了解，不落地。refdocs 的「双通道向量融合（user 塔 + fuse）」实践中零调用已删——个性化走「偏好词并入检索词 + payload filter」。
- **eval/**（`refdocs/08`）：Rubrics as Rewards —— P0 业务红线（一票否决）/ P1 执行规范（扣分）/ P2 质量打分（1-5）；每条 query 动态生成评分细则 + judge 模型打分，用于离线评测与 bad case 定位。**本项目只做评测，不做 SFT / RL 训练与微调**（refdocs 08 里训练相关内容仅作背景了解，不落地）。数据飞轮的训练腿需 GPU 故砍掉，但保留**上下文级飞轮**：评测 → 定位 bad case → 改 prompt/工具 / 沉淀 few-shot 示例 → 再评测对照（零 GPU 迭代闭环，详见 ROADMAP M11）。

---

## 8. 配置（.env）

至少包含：LLM（`OPENAI_BASE_URL` / `OPENAI_API_KEY` / `LLM_MAIN` / `LLM_JUDGE`）、向量召回（`EMBED_MODEL` / `EMBED_BASE_URL` / `EMBED_API_KEY` / `QDRANT_URL` / `QDRANT_COLLECTION`）、平台 API key、`TAVILY_API_KEY`、缓存标记开关（`COMPRESS_CACHE_CONTROL`）。长期记忆 Store 无需单独配置（随 `DATABASE_URL` 走 SQLite，见 `app/memory/store.py`）。全量键以 `.env.example` 为准。用 `python-dotenv` 加载。

---

## 9. 常用命令

```bash
uv sync                              # 安装依赖（pyproject.toml）
uv run python scripts/fetch_milistu.py   # 拉取 RAG 数据集（产物在 data/，已 gitignore）
uv run uvicorn app.api.server:app --reload   # 起后端（实现 server.py 后）
cd frontend && npm install && npm run dev     # 起前端（创建 frontend 后）
uv run pytest                        # 跑测试

# 数据库迁移（M17，账户库 users/threads）
uv run alembic revision --autogenerate -m "说明"   # 改完 app/db/models.py 后生成迁移
uv run alembic upgrade head                        # 手动升级（应用启动时 init_db() 也会自动跑）
uv run alembic downgrade -1                        # 回滚一步
```

> **改表结构 = 改 `app/db/models.py` + 生成一条迁移**，两件事一起做。光改模型不生成迁移，本地看着正常
> （测试库每次重建），线上那份老库却不会跟着变。生成后**务必读一遍** autogenerate 的产物：它检得出加列
> /删列/改类型，但检不出「重命名」（会当成 drop + add，数据直接没）。基线 `0001_accounts` 别改动——
> 线上老库是 stamp 到它的，见 `app/db/session.py`。

`data/`、`*.csv`、`*.parquet`、`.venv/` 已在 `.gitignore`，数据靠 `scripts/` 复现，不入库。

---

## 10. 开发路线（章节映射）

教学分两段，建议按此顺序落地，**先备装备（能力示例进 `examples/`）再打 boss（主线进 `app/`）**。**可执行的里程碑清单见根目录 [`ROADMAP.md`](./ROADMAP.md)**（按工程依赖排，含产出物与验收标准）。

| 章 | 主题 | 落地物 |
| --- | --- | --- |
| 01-02 | AgentLoop 范式 / 最小示例 | `examples/` 跑通 Think→Act→Observe→Reflect |
| 03 | 三件事判断 / 读写切分 | `dispatch_tool` + `permissions.py` |
| 04 | 三塔向量召回 | `recall/towers.py` `ann.py` |
| 05 | Cache Breakpoint | `harness/formatter.py`（压缩交框架 `compress_context`） |
| 06 | 长期记忆 Store | `memory/` |
| 07 | AGUI + WebSocket | `api/monitor.py` `connection.py` |
| 08 | Rubric 评测（不做训练/微调） | `eval/` |
| 09 | 项目总览 / 工程初始化 | 建目录骨架、`pyproject.toml` |
| 10 | 基础底座 | `.env` / `llm.py` / `context.py` / `utils/` |
| 11-13 | 三个跨平台子 Agent | `tools/` 各业务工具 + `recall/` RAG |
| 14 | 主 Agent 组装 | `agents.py` / `orchestrator.py` + Supervisor-Workers 协同 |
| 15 | FastAPI + 前端闭环 | `api/server.py` + `frontend/` |

---

## 11. 给 Claude 的提示

- 动手实现任一模块前，**先读对应 `refdocs/` 章节**（见第 10 节映射）：**学它的设计思想与框架，别照抄它的代码**。refdocs 代码是扒来的、可能残缺/过时/有错——遇到跑不通或不合理处，按真实依赖与本仓库数据**主动更正**，保持框架一致即可。
- 框架层（第 2 节核心架构 + 各硬约束）要与 refdocs 对齐；实现层（签名/常量/字段/流程）以「能跑、自洽、合理」为准，本文件给的只是建议初值。
- 读写切分是核心：worker 的能力边界靠 **Toolkit 发放范围 + `is_read_only` 标记 + `PermissionEngine` 精准放行**这三样结构性保证，不靠 prompt 劝退；主 Agent 单干优先，别把小事也派一趟。
- 永远先实现派发安全四层与 `<termination>`，否则容易无限递归 / 死循环 / token 爆炸。
- 工具一律 async + `ToolChunk` 返回 + 保留 `*Output` schema + AGUI 上报；新增工具记得加进 `BUSINESS_TOOLS` 并在 `build_toolkit(role)` 里决定发给谁。
- 数据文件勿入库，靠 `scripts/` 复现。
